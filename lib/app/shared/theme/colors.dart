import 'package:flutter/material.dart';

const primary = Color(0xFF0F766A);
const primaryDark = Color(0xFF165867);
const secondary = Color(0xFF1B3F65);

const gradientButton = LinearGradient(
  colors: [
    primary,
    primaryDark,
  ],
);

const gradientGrey = LinearGradient(
  colors: [
    Color(0xFFE2E8F0),
    Color(0xFFF1F5F9),
  ],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);

const primary20 = Color.fromARGB(70, 15, 118, 106);
const black = Color(0xFF000000);
const white = Color(0xFFFFFFFF);
const grey = Color(0xFF8E8E8E);
const grey20 = Color.fromARGB(60, 142, 142, 142);

const slate100 = Color(0xFFF1F5F9);
const slate900 = Color(0xFF0F172A);
